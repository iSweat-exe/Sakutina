export {
    AVAILABLE_JOBS,
    STREAK_BONUS_PER_DAY,
    STREAK_BONUS_CAP,
    SHOP_ITEMS,
} from '@sakutina/economy';

/** Daily reward amount (coins) */
export const DAILY_REWARD = 500;

/** One-time balance grant on a player's very first interaction in a guild. */
export const WELCOME_BONUS = 500;
