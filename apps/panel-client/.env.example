# Empty/relative in dev â€” the Vite dev server proxies /auth and /api to
# panel-server (see vite.config.ts), so the browser sees them as same-origin.
# Set this to panel-server's real origin once there is a production deployment.
VITE_API_BASE_URL=


