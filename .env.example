# This is a monorepo - each app owns its own env file. Copy the one(s) you need:
#
#   apps/bot/.env.example           - the Discord bot
#   apps/panel-server/.env.example  - the web panel's API (Hono)
#   apps/panel-client/.env.example  - the web panel's frontend (Vite)
#
# apps/bot and apps/panel-server both point at the same DATABASE_URL and
# share the same DISCORD_TOKEN / CLIENT_ID (one Discord application, two
# processes reading from the same Postgres instance via packages/db).


